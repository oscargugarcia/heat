% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_data.R
\name{check_raster}
\alias{check_raster}
\title{Check and Validate Environmental Raster for Pipeline Compatibility}
\usage{
check_raster(raster, raster_name = "raster", lon_tolerance = 181, verbose = 1)
}
\arguments{
\item{raster}{A SpatRaster object to validate}

\item{raster_name}{A character string describing the raster (for error/info messages)}

\item{lon_tolerance}{Numeric tolerance for detecting 0-360 degree format (default: 181).
Rasters with xmax > lon_tolerance are considered to be in 0-360 format.}
}
\value{
The validated raster with properly formatted layer names and coordinate system
}
\description{
This function performs comprehensive validation of environmental rasters to ensure they are
suitable for use in the r2e2 pipeline. It checks:
\itemize{
\item Layer names are in valid date format (via check_raster_layers)
\item Longitude coordinates are in -180 to 180 degree format (not 0-360)
}

If issues are found, the function provides clear guidance on how to fix them.
}
\examples{
\dontrun{
  # Check an existing SpatRaster object
  checked_raster <- check_raster(my_raster, raster_name = "ERA5 Temperature")
  
  # If raster is in 0-360 format, you'll get an error with instructions:
  # Error: Raster uses 0-360 degree longitude format (xmin=0.5, xmax=359.5)
  # The pipeline requires -180 to 180 degree format.
  # 
  # To fix this, rotate the raster before using it:
  #   library(terra)
  #   env_rast <- rast("your_file.nc")
  #   env_rast <- rotate(env_rast)
  #   result <- r2e2(env_rast = env_rast, ...)
}
}
