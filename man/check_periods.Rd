% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregation.R
\name{check_periods}
\alias{check_periods}
\title{Check Weighting Periods and Secondary Weight Rasters}
\usage{
check_periods(
  env_rast_list,
  sec_weight_layer_names = NULL,
  sec_weights = TRUE,
  sec_weight_name,
  verbose = 1
)
}
\arguments{
\item{env_rast_list}{A list of environmental raster subsets, one per weighting period.}

\item{sec_weight_layer_names}{(Optional) A character vector of secondary weight raster layer names. Required if sec_weights = TRUE.}

\item{sec_weights}{Logical flag indicating whether to use secondary weight rasters. Default is TRUE}

\item{sec_weight_name}{Name of the secondary weight product for display purposes}
}
\value{
A data.frame verification table with columns:
\itemize{
\item Period: The name of the period.
\item Date_Range: The date range for that period (derived from the raster layer names).
\item Secondary_Weight_Raster: The corresponding secondary weight raster layer name (or NA if sec_weights = FALSE).
}
}
\description{
This function verifies that the number of environmental raster periods matches the number of
secondary weight raster layers when secondary weighting is requested. If \code{sec_weights} is TRUE, it
stops with an error if the counts do not match and then generates a verification table displaying
each period's name, date range, and the corresponding secondary weight raster layer. If \code{sec_weights}
is FALSE, it prints a message stating that no secondary weighting was requested and creates the
verification table with the Secondary_Weight_Raster column left empty.
}
\examples{
\dontrun{
  # When secondary weighting is used:
  check_periods(env_rast_list, sec_weight_layer_names, sec_weights = TRUE)

  # When secondary weighting is not used:
  check_periods(env_rast_list, sec_weights = FALSE)
}
}
